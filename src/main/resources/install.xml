<install>
  <update file="${package.root}/install/bundles" todir="${env.bundles}" />
  <copy dir="${package.root}/templates" todir="${env.templates}" overwrite="true" />
  <config addtemplate="openid" />
</install>
